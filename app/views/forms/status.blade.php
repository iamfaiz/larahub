{{ Form::open() }}
	<div class="form-group">
		<textarea name="body" placeholder="What's on your mind?" class="form-control" required="true"></textarea>
	</div>
	<div class="form-group">
			<button class="btn btn-primary">Update Status</button>
	</div>
{{ Form::close() }}
